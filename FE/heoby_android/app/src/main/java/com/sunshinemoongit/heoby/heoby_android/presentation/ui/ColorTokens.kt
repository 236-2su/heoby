package com.sunshinemoongit.heoby.heoby_android.presentation.ui

import androidx.compose.ui.graphics.Color

/** 그라데이션 시작 색상 */
val GradientStart = Color(0xFFEEEDEC)
/** 그라데이션 끝 색상 */
val GradientEnd = Color(0xFFF0E6C6)

/** 카드/표면 배경 (연한 베이지톤) */
val SurfacePrimary = Color(0xFFFFFBF2)
/** 카드 그림자나 보조 배경 (살짝 어두운 톤) */
val SurfaceSecondary = Color(0xFFF3EBDD)
/** 아주 밝은 하이라이트 영역용 */
val SurfaceHighlight = Color(0xFFFFFEFA)

/** =========================
 * 🖋 텍스트
 * ========================= */

/** 기본 텍스트 색상 (기존 유지) */
val TextPrimary = Color(0xFF2B2119)
/** 보조 텍스트 색상 (기존 유지) */
val TextSecondary = Color(0xFF62574C)
/** 반투명 텍스트 (비활성, placeholder 등) */
val TextDisabled = Color(0x9962574C)
/** 강조 텍스트 (하이라이트용) */
val TextAccent = Color(0xFF9C4F2F)

/** =========================
 * ✨ 포인트 / 강조
 * ========================= */

/** 주요 포인트 컬러 (기존 유지) */
val AccentPrimary = Color(0xFF9C4F2F)
/** 비활성 강조 색상 (기존 유지) */
val AccentDisabled = Color(0xFFB6AAA1)
/** 부드러운 포인트 (예: 버튼 hover, ripple 등) */
val AccentSoft = Color(0xFFE8D6C9)

/** =========================
 * ❤️ 상태 / 심각도
 * ========================= */

/** 정상 (녹색 계열, 기존 유지) */
val SeverityNormal = Color(0xFF2E7D32)
/** 경고 (노랑 계열, 기존 유지) */
val SeverityWarning = Color(0xFFF9A825)
/** 위험 (빨강 계열, 기존 유지) */
val SeverityCritical = Color(0xFFC62828)

/** 주의 (orange 계열, warning과 critical 사이 톤) */
val SeverityAlert = Color(0xFFEF6C00)
/** 정보 (파랑 계열, 상태 표시용) */
val SeverityInfo = Color(0xFF1976D2)

/** =========================
 * ⚙️ 경계선 / Divider / Stroke
 * ========================= */

/** 기본 구분선 색상 */
val BorderDefault = Color(0xFFD8CFC3)
/** 강조 구분선 색상 */
val BorderAccent = Color(0xFF9C4F2F)
/** 카드 외곽선 (중간톤) */
val BorderSurface = Color(0xFFE5DDD0)

/** =========================
 * 🌙 다크모드 대응 (선택)
 * ========================= */

/** 다크 배경 */
val DarkBackground = Color(0xFF1B1A17)
/** 다크 표면 */
val DarkSurface = Color(0xFF25231F)
/** 다크 텍스트 */
val DarkTextPrimary = Color(0xFFF4EADB)
/** 다크 강조 */
val DarkAccent = Color(0xFFCC8866)